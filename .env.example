# Copy this file to .env and fill in real values before running
# `docker compose up -d --build`. .env itself is gitignored - never commit it.

POSTGRES_USER=pods2
POSTGRES_PASSWORD=change-me-to-a-real-password
POSTGRES_DB=pods2
